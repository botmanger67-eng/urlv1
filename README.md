# urlv1
A URL shortener web app built with Flask and SQLite. Users can submit a long URL to get a shortened version, which redirects to the original URL. Each shortened URL tracks click counts. The frontend uses Bootstrap for a clean, responsive design.
